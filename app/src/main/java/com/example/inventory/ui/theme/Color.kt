package com.example.inventory.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * 应用颜色定义
 * 
 * 设计风格：蓝色主题 + 粉色强调色
 * 参考：现代简洁设计风格
 */

// ==================== 主色调 - 蓝色系 ====================

/**
 * 主色 - 蓝色 (#7b8cdb)
 * 用于：顶部栏、主要按钮、链接
 */
val PrimaryBlue = Color(0xFF7B8CDB)

/**
 * 主色变体 - 浅蓝色
 * 用于：悬停状态、渐变
 */
val PrimaryBlueVariant = Color(0xFF8C9DE5)

/**
 * 主色容器 - 极浅蓝色
 * 用于：选中状态背景
 */
val PrimaryBlueContainer = Color(0xFFE8EBFA)

// ==================== 强调色 - 粉色系 ====================

/**
 * 强调色 - 粉红色
 * 用于：FAB、徽章、重要提示
 */
val AccentPink = Color(0xFFFF4081)

/**
 * 强调色变体 - 浅粉色
 * 用于：悬停状态
 */
val AccentPinkVariant = Color(0xFFFF6090)

/**
 * 强调色容器 - 极浅粉色
 * 用于：粉色元素的背景
 */
val AccentPinkContainer = Color(0xFFFFE0E9)

// ==================== 背景色系 ====================

/**
 * 页面背景色 - 浅灰白色
 * 用于：页面主背景
 */
val BackgroundLight = Color(0xFFF5F5F5)

/**
 * 表面背景色 - 纯白色
 * 用于：卡片、对话框背景
 */
val SurfaceWhite = Color(0xFFFFFFFF)

/**
 * 表面变体 - 浅灰色
 * 用于：分隔线、禁用状态
 */
val SurfaceVariant = Color(0xFFE0E0E0)

// ==================== 文字颜色系 ====================

/**
 * 主要文字颜色 - 深灰色
 * 用于：标题、正文
 */
val TextPrimary = Color(0xFF333333)

/**
 * 次要文字颜色 - 中灰色
 * 用于：辅助信息、说明文字
 */
val TextSecondary = Color(0xFF666666)

/**
 * 提示文字颜色 - 浅灰色
 * 用于：占位符、禁用文字
 */
val TextHint = Color(0xFF999999)

/**
 * 白色文字
 * 用于：深色背景上的文字
 */
val TextWhite = Color(0xFFFFFFFF)

// ==================== 功能色系 ====================

/**
 * 成功色 - 绿色
 * 用于：成功提示、确认按钮
 */
val SuccessGreen = Color(0xFF4CAF50)

/**
 * 警告色 - 橙色
 * 用于：警告提示、库存不足
 */
val WarningOrange = Color(0xFFFF9800)

/**
 * 错误色 - 红色
 * 用于：错误提示、删除按钮
 */
val ErrorRed = Color(0xFFF44336)

/**
 * 信息色 - 蓝色
 * 用于：信息提示
 */
val InfoBlue = Color(0xFF2196F3)

// ==================== 分隔线和边框 ====================

/**
 * 分隔线颜色
 */
val Divider = Color(0xFFE0E0E0)

/**
 * 边框颜色
 */
val Border = Color(0xFFD0D0D0)

/**
 * 浅边框颜色
 */
val BorderLight = Color(0xFFF0F0F0)
