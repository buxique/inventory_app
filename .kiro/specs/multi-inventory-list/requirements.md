# 多库存列表管理功能需求规范

**功能名称**: 多库存列表管理系统  
**优先级**: P0 - 核心功能  
**创建日期**: 2026-01-25  
**状态**: 需求分析阶段

---

## 1. 功能概述

实现多库存列表管理系统，允许用户创建、管理和切换多个独立的库存列表，每个列表可以独立命名、导入数据和管理商品。

---

## 2. 用户故事

### 2.1 列表内新增商品

**作为** 库存管理员  
**我想要** 在商品列表底部看到一个"+"新增按钮  
**以便于** 快速添加新商品到当前列表

**验收标准**:
- [ ] 在商品列表最后一个商品下方显示"+新增"按钮
- [ ] 点击按钮弹出手动添加商品对话框
- [ ] 功能与空列表时的粉色"添加商品"按钮一致
- [ ] 按钮样式与现有UI风格保持一致

### 2.2 创建新库存列表

**作为** 库存管理员  
**我想要** 通过右下角"+"按钮创建新的库存列表  
**以便于** 管理不同仓库或分类的库存

**验收标准**:
- [ ] 右下角"+"按钮弹出对话框，标题为"导入或新建库存列表"
- [ ] 对话框包含选项：手动添加、导入Excel、导入Access、导入数据库
- [ ] 移除原有的"手动添加商品"和"拍照添加"选项
- [ ] 选择任一选项立即创建新的空白库存列表
- [ ] 新列表样式与第一个列表一致

### 2.3 库存列表命名

**作为** 库存管理员  
**我想要** 为每个库存列表设置自定义名称  
**以便于** 区分不同的仓库或分类

**验收标准**:
- [ ] 新建列表默认命名为"仓库1"、"仓库2"、"仓库3"等
- [ ] 用户可以自定义修改列表名称
- [ ] 列表名称显示在页面左上角
- [ ] 名称长度限制：1-20个字符

### 2.4 库存列表切换 - 滑动

**作为** 库存管理员  
**我想要** 通过左右滑动切换不同的库存列表  
**以便于** 快速浏览不同仓库的库存

**验收标准**:
- [ ] 支持左右滑动手势切换列表
- [ ] 滑动时显示切换动画
- [ ] 显示当前列表位置指示器（如 1/3）
- [ ] 滑动到边界时有视觉反馈

### 2.5 库存列表切换 - 下拉菜单

**作为** 库存管理员  
**我想要** 点击左上角下拉按钮选择库存列表  
**以便于** 精确跳转到指定列表

**验收标准**:
- [ ] 左上角显示当前列表名称 + 下拉图标
- [ ] 点击弹出列表选择对话框
- [ ] 对话框显示所有列表名称
- [ ] 选择列表后立即切换
- [ ] 当前列表有视觉标识（如勾选标记）

### 2.6 文件导入流程

**作为** 库存管理员  
**我想要** 选择导入选项时自动打开文件浏览器  
**以便于** 快速导入库存数据

**验收标准**:
- [ ] 选择"导入Excel/Access/数据库"时创建空白列表
- [ ] 同时弹出文件浏览器选择文件
- [ ] 支持的文件格式：.xlsx, .xls, .mdb, .accdb, .db
- [ ] 导入过程显示进度条
- [ ] 导入完成后显示成功/失败消息
- [ ] 导入失败时保留空白列表

### 2.7 导入进度显示

**作为** 库存管理员  
**我想要** 在导入大文件时看到进度条  
**以便于** 了解导入进度和预计完成时间

**验收标准**:
- [ ] 导入开始时显示进度对话框
- [ ] 显示百分比进度（0-100%）
- [ ] 显示已导入/总数量（如 50/200）
- [ ] 支持取消导入操作
- [ ] 导入时间超过2秒时显示进度条

---

## 3. 功能需求

### 3.1 数据库架构

#### 3.1.1 新增表：inventory_lists

```sql
CREATE TABLE inventory_lists (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    display_order INTEGER NOT NULL DEFAULT 0,
    created_at INTEGER NOT NULL,
    last_modified INTEGER NOT NULL,
    is_default INTEGER NOT NULL DEFAULT 0
);
```

**字段说明**:
- `id`: 列表唯一标识
- `name`: 列表名称（如"仓库1"）
- `display_order`: 显示顺序
- `created_at`: 创建时间戳
- `last_modified`: 最后修改时间戳
- `is_default`: 是否为默认列表

#### 3.1.2 修改表：inventory_items

添加外键关联到库存列表：

```sql
ALTER TABLE inventory_items ADD COLUMN list_id INTEGER NOT NULL DEFAULT 1;
CREATE INDEX idx_items_list_id ON inventory_items(list_id);
```

### 3.2 UI组件

#### 3.2.1 列表底部新增按钮

```kotlin
@Composable
fun AddItemButton(
    onClick: () -> Unit,
    modifier: Modifier = Modifier
) {
    // 显示在列表底部的"+"新增按钮
}
```

#### 3.2.2 列表切换器（ViewPager）

```kotlin
@Composable
fun InventoryListPager(
    lists: List<InventoryList>,
    currentListId: Long,
    onListChange: (Long) -> Unit,
    content: @Composable (InventoryList) -> Unit
) {
    // 支持左右滑动的列表切换器
}
```

#### 3.2.3 列表选择下拉菜单

```kotlin
@Composable
fun InventoryListDropdown(
    lists: List<InventoryList>,
    currentListId: Long,
    onListSelect: (Long) -> Unit,
    onRenameList: (Long, String) -> Unit
) {
    // 列表选择和重命名对话框
}
```

#### 3.2.4 导入进度对话框

```kotlin
@Composable
fun ImportProgressDialog(
    visible: Boolean,
    progress: Float,
    currentCount: Int,
    totalCount: Int,
    onCancel: () -> Unit
) {
    // 显示导入进度的对话框
}
```

### 3.3 ViewModel扩展

#### 3.3.1 InventoryListViewModel

```kotlin
class InventoryListViewModel(
    private val listRepository: InventoryListRepository
) : ViewModel() {
    
    val lists: StateFlow<List<InventoryList>>
    val currentListId: StateFlow<Long>
    
    fun createList(name: String? = null)
    fun deleteList(listId: Long)
    fun renameList(listId: Long, newName: String)
    fun switchToList(listId: Long)
    fun getNextDefaultName(): String  // "仓库1", "仓库2", ...
}
```

#### 3.3.2 ImportViewModel

```kotlin
class ImportViewModel(
    private val importCoordinator: ImportCoordinator
) : ViewModel() {
    
    val importProgress: StateFlow<ImportProgress>
    
    suspend fun importFile(
        listId: Long,
        fileUri: Uri,
        fileType: FileType
    ): Result<Int>
    
    fun cancelImport()
}
```

---

## 4. 非功能需求

### 4.1 性能要求

- 列表切换响应时间 < 300ms
- 导入1000条数据 < 5秒
- 滑动切换流畅度 ≥ 60fps

### 4.2 数据迁移

- 现有数据自动迁移到"仓库1"列表
- 迁移过程对用户透明
- 迁移失败时有回滚机制

### 4.3 兼容性

- 支持Android 8.0+
- 支持不同屏幕尺寸
- 支持深色/浅色主题

---

## 5. 约束条件

### 5.1 技术约束

- 使用Room数据库
- 使用Jetpack Compose UI
- 使用Kotlin协程处理异步操作

### 5.2 业务约束

- 最多支持50个库存列表
- 每个列表最多10,000个商品
- 列表名称不能重复

---

## 6. 用户界面设计

### 6.1 顶部栏设计

```
┌─────────────────────────────────────┐
│ 仓库1 ▼  🔍  📁  ⚙️                │
└─────────────────────────────────────┘
   ↑
   点击弹出列表选择对话框
```

### 6.2 列表底部按钮

```
┌─────────────────────────────────────┐
│ 商品1                               │
│ 商品2                               │
│ 商品3                               │
│                                     │
│        ┌─────────────┐              │
│        │  + 新增     │              │
│        └─────────────┘              │
└─────────────────────────────────────┘
```

### 6.3 右下角FAB对话框

```
┌─────────────────────────────────────┐
│ 导入或新建库存列表                   │
├─────────────────────────────────────┤
│ 手动添加                            │
│ 导入 Excel                          │
│ 导入 Access                         │
│ 导入数据库文件                       │
└─────────────────────────────────────┘
```

### 6.4 列表选择对话框

```
┌─────────────────────────────────────┐
│ 选择库存列表                         │
├─────────────────────────────────────┤
│ ✓ 仓库1                             │
│   仓库2                             │
│   仓库3                             │
│                                     │
│ [+ 新建列表]  [重命名]  [删除]      │
└─────────────────────────────────────┘
```

### 6.5 导入进度对话框

```
┌─────────────────────────────────────┐
│ 正在导入...                         │
├─────────────────────────────────────┤
│ ████████████░░░░░░░░  60%          │
│                                     │
│ 已导入: 120 / 200                   │
│                                     │
│           [取消导入]                 │
└─────────────────────────────────────┘
```

---

## 7. 实施阶段

### 阶段1: 数据库架构（P0）
- [ ] 创建 inventory_lists 表
- [ ] 修改 inventory_items 表添加 list_id
- [ ] 实现数据迁移逻辑
- [ ] 编写数据库测试

### 阶段2: Repository层（P0）
- [ ] 创建 InventoryListRepository
- [ ] 修改 InventoryRepository 支持 list_id 过滤
- [ ] 实现列表CRUD操作
- [ ] 编写Repository测试

### 阶段3: ViewModel层（P0）
- [ ] 创建 InventoryListViewModel
- [ ] 修改 InventoryViewModel 支持多列表
- [ ] 创建 ImportViewModel
- [ ] 编写ViewModel测试

### 阶段4: UI组件（P0）
- [ ] 实现列表底部新增按钮
- [ ] 实现列表切换器（ViewPager）
- [ ] 实现列表选择下拉菜单
- [ ] 实现导入进度对话框

### 阶段5: 集成测试（P0）
- [ ] 端到端测试
- [ ] 性能测试
- [ ] 用户体验测试
- [ ] Bug修复

---

## 8. 风险评估

### 8.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| 数据迁移失败 | 高 | 中 | 实现回滚机制，充分测试 |
| 性能下降 | 中 | 中 | 使用索引，优化查询 |
| UI复杂度增加 | 中 | 高 | 渐进式重构，保持简洁 |

### 8.2 用户体验风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| 学习曲线陡峭 | 中 | 中 | 提供引导教程 |
| 误操作删除列表 | 高 | 低 | 添加确认对话框 |
| 导入失败数据丢失 | 高 | 低 | 保留原文件，支持重试 |

---

## 9. 测试计划

### 9.1 单元测试

- [ ] InventoryListRepository 测试
- [ ] InventoryListViewModel 测试
- [ ] ImportViewModel 测试
- [ ] 数据迁移逻辑测试

### 9.2 集成测试

- [ ] 创建列表流程测试
- [ ] 切换列表流程测试
- [ ] 导入数据流程测试
- [ ] 删除列表流程测试

### 9.3 UI测试

- [ ] 列表底部按钮显示测试
- [ ] 滑动切换列表测试
- [ ] 下拉菜单选择测试
- [ ] 进度条显示测试

---

## 10. 后续优化

### 10.1 短期优化（1-2周）

- 列表排序功能
- 列表图标自定义
- 列表统计信息（商品数量、总价值等）

### 10.2 中期优化（1-2月）

- 列表间商品移动/复制
- 列表导出功能
- 列表模板功能

### 10.3 长期优化（3-6月）

- 多用户协作
- 云端同步
- 列表权限管理

---

## 11. 参考资料

- Material Design 3 - Navigation
- Android ViewPager2 文档
- Room数据库迁移指南
- Jetpack Compose状态管理

---

## 12. 变更记录

| 日期 | 版本 | 变更内容 | 作者 |
|------|------|---------|------|
| 2026-01-25 | 1.0 | 初始版本 | Kiro |

---

## 13. 审批

- [ ] 产品经理审批
- [ ] 技术负责人审批
- [ ] UI/UX设计师审批
- [ ] 开发团队评审
